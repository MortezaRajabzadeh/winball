import 'package:flutter/material.dart';

class WalletIconButtonWidget extends StatelessWidget {
  const WalletIconButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: const Icon(
        Icons.wallet_giftcard,
      ),
      onPressed: () {},
    );
  }
}
