enum SizeDirection {
  vertical,
  horizontal,
}

enum DialogStatus {
  open,
  closed,
}

enum DialogType {
  loading,
  error,
}

enum PageType {
  home,
  slider,
  siteSetting,
  levels,
  withdraws,
  transactions,
  announcements,
  activities,
  helps,
  users,
  walletManagement,
}

enum PopupMenuItemOptions {
  edit,
  remove,
  details,
}
