name: announcement_repository
description: a repository to do crud on announcements
publish_to: none
version: 1.0.0+1
environment:
  sdk: ^3.5.0
dependencies:
  base_repository:
    path: ../base_repository/
  network_repository:
    path: ../network_repository/
  user_repository:
    path: ../user_repository/

  